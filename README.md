```
# serial2serial
Simple bi-directional serial to serial passthrough (proxy) with optional pcap capture.

Standalone program that can be put into a service.

Usage:
serial2serial -a [uart1_path] -b [uart2_path] -s [speed] [-d]
```
